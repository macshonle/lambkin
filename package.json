{
  "name": "lambkin",
  "version": "0.1.0",
  "private": true,
  "description": "Lambda Calculus evaluator and LSP",
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "watch": "pnpm -r --parallel watch",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "package": "pnpm --filter lambkin-vscode package"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
